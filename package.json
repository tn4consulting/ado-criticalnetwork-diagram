{
  "name": "critical-network-diagram",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Epic dependency network + Gantt viewer, sourced from Azure DevOps.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "fetch": "node fetch_epics.js --project BDM --project BAD --project DECD --project BDM2 --project TPLAT",
    "build": "node build.js",
    "sync": "npm run fetch && npm run build",
    "dev": "npx --yes http-server src -p 8080 -c-1",
    "seed": "node seed/seed_ado_epics.js",
    "seed:apply": "node seed/seed_ado_epics.js --apply",
    "tag": "node seed/tag_epics.js"
  }
}
